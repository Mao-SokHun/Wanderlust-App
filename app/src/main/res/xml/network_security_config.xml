<?xml version="1.0" encoding="utf-8"?>
<!-- Production Network Security Configuration for Wanderlust -->
<!-- Enforces HTTPS / TLS encrypted connections globally, protecting against Man-In-The-Middle (MITM) attacks on public Wi-Fi -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- Allow local dev emulator loopback only -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
